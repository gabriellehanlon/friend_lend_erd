<%= form_with(model: @saved_item, local: true) do |f| %>

  <%= f.hidden_field :item_id, value: @item.id %>

  <%= f.hidden_field :user_id, value: current_user.id %>

  <%= f.submit "Create saved item", class: "btn btn-block btn-outline-secondary" %>
<% end %>
